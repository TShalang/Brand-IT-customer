@model IEnumerable<Kimo.Models.Template_Position>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Position.Position_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Template.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Position.Position_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Template.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Template_Position_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Template_Position_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Template_Position_ID })
        </td>
    </tr>
}

</table>
