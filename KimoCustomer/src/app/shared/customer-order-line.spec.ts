import { CustomerOrderLine } from './customer-order-line';

describe('CustomerOrderLine', () => {
  it('should create an instance', () => {
    expect(new CustomerOrderLine()).toBeTruthy();
  });
});
